<%= link_to(question_path(question)) do %>
  <div class="container-new2">
    <p><%= question.title %></p>
    <p><%= question.content %></p>
    <p><%= l(question.created_at, format: :long) %></p>
  </div>
<% end %>
