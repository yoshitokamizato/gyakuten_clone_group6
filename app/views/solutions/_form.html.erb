<%= form_with model: [@question, @solution] do |form| %>
  <%= form.text_area :answer, class: 'form-control', rows: 10 %>
  <%= form.submit "送信", class: "btn btn-primary btn-block btn-lg", data: { confirm: "投稿しますか？" } %>
<% end %>
