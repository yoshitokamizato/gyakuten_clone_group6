<%= form_with model: [@question, @solution] do |form| %>
  <%= form.text_area :answer, class: "form-control my-3", rows: 10 %>
  <%= form.submit "送信", class: "btn btn-primary btn-block btn-lg mb-5", data: { confirm: "投稿しますか？" } %>
<% end %>
